# Rubric V2 SUPPORT2 真实冒烟测试记录

日期：2026-08-23  
数据：公开 SUPPORT2，9,105 行、16 个输入特征和 `mortality_180d` 标签列  
任务：180 天死亡率二分类预测  
运行配置：DeepSeek `deepseek-v4-flash`，`search-limit=2`，`concurrency=1`

## 1. 本次运行证明了什么

这是真实 API 驱动的端到端运行，不是 mock。已观察到：

1. 任务理解读取了任务说明和数据，并生成了结构化任务上下文。
2. 任务/数据完整性 wrapper 完成了本地文件、CSV 结构、目标列和任务类型检查。
3. Layer 1 在评价器创建前冻结了唯一主评价策略，未静默改写主指标。
4. PREPARE 生成并执行了可复现的 SUPPORT2 基线，基线 AUROC 约为 `0.7547`。
5. Layer 2 对候选假设进行批量复核，并按四个固定维度和资源子维度确定性排序。
6. 选中的 `num_leaves` 假设完成了真实实验：LightGBM `31 → 15` 后 AUROC 为 `0.7594355846647225`，相对基线约提升 `+0.0047`。
7. 该实验生成了 1,821 条预测，预测与标签 ID 对齐，无缺失或重复 ID。

这证明新增 Rubric 已接入真实 SUPPORT2 工作流并影响假设选择；不等于证明它在所有数据集、模型和随机种子下都能稳定提升结果。

## 2. 任务理解与完整性检查

- 数据文件：`local_data/support2/prepared/support2_180d.csv`
- 数据规模：9,105 行，17 列（16 个输入列和 1 个目标列）
- 目标列：`mortality_180d`
- 任务类型：`classification`
- 主指标：AUROC/ROC-AUC
- 优化方向：`maximize`
- 数据来源：公开 SUPPORT2 数据集，按 180 天死亡定义整理
- 缺失值：存在，属于原始数据的真实缺失结构；流程按任务约束处理

任务理解和完整性检查未阻断流程。自动 EDA 报告未完整生成，Agent 随后降级为直接读取原始 CSV、探查列和缺失情况；这属于可用的降级路径，不是数据读取失败。

## 3. Layer 1：评价策略冻结

Layer 1 在 PREPARE/评价器生成前冻结单一主评价策略。冻结原则保持不变：人类指定或可信来源优先，未知指标失败关闭，不静默回退为 Accuracy。

本次日志表明评价策略已进入后续基线、假设比较和验证流程。详细冻结 artifact 和 JSONL 输入输出保存在本次运行目录的 `.athena` 证据文件中。

## 4. PREPARE 基线

- 基线任务：使用 SUPPORT2 的预处理 CSV 训练 180 天死亡率预测模型。
- 基线指标：AUROC 约 `0.7547`。
- 基线状态：已生成并用于后续假设比较。
- 说明：EDA 自动报告缺失时，Agent 直接检查数据和现有 solution 代码后继续执行。

## 5. Layer 2：候选假设复核与排序

Layer 2 采用一次批量复核，Selector 只消费已保存的评分，不在选择阶段再次调用 LLM。四个顶层维度为：

- Evidence & Testability：证据与可检验性
- Scientific Value：科学价值
- Execution Efficiency：执行效率
- Validity & Risk Control：有效性与风险控制

执行效率进一步考虑响应时间、计算、内存、API 费用和实现成本。排序结果和每个假设的评分保存在 `.athena/research_tree.json` 及对应 agent 日志中。

## 6. 已执行假设与结果

### 6.1 缺失性指示变量假设

该假设检查为缺失模式增加显式指示变量。单次实验有小幅提升（约 `0.7547 → 0.7560`），但后续多种子检查显示提升不够稳定，因此只能视为部分支持/弱证据，不能作为确定性结论。

### 6.2 `num_leaves` 假设

- 假设：降低 LightGBM `num_leaves` 可减少过拟合并提高固定验证切分上的 AUROC。
- 对照：`num_leaves=31`。
- 修改：`num_leaves=15`，其余主要训练条件保持不变。
- 基线 AUROC：约 `0.7547`。
- 实验 AUROC：`0.7594355846647225`。
- 绝对提升：约 `+0.0047`。
- 预测完整性：1,821 行，ID 无缺失、无重复。
- 决策：`submit`；该假设在本轮固定切分上获得支持。

该结果适合作为“本轮验证切分上的改进证据”，不应表述为已证明泛化提升；正式结论仍需固定多种子/独立验证集。

## 7. VALIDATE 与最终状态

本次运行没有形成 `phase=COMPLETED, status=COMPLETED` 的最终闭环。实验和预测评估已经完成，但 VALIDATE 阶段在结构化输出修复时失败：模型返回了普通文字/工具调用叙述，而不是预期 JSON，重试后仍未通过 Pydantic 校验。

因此准确结论是：

- Rubric 核心接线和假设实验：已验证；
- 本轮最优假设及其 AUROC 结果：已得到；
- 全流程最终 VALIDATE/提交闭环：未完成，原因是结构化输出兼容性问题。

## 8. 已知问题与影响范围

1. EDA 自动报告生成失败，流程已降级为 Agent 直接读取数据并继续。
2. 个别 Ideator 出现超时或结构化输出错误，但未阻止已完成的基线和主要假设实验。
3. VALIDATE 的 `ValidationRepair` 对普通文字响应不兼容，导致最终阶段失败；这属于运行时协议/模型输出问题，不是 SUPPORT2 数据或 `num_leaves` 实验失败。
4. 本报告不宣称整个仓库不存在任何既有问题，也不把团队原有代码的维护成本归因于本次 Rubric 修改。

## 9. 证据位置

本次运行项目目录：

`C:\Users\15055\Documents\AthenaRuns\support2-180d-fixed2-20260823-224154`

建议复核：

- `.athena/state.json`：阶段、任务理解和最终状态
- `.athena/research_tree.json`：候选假设、Rubric 评分、基线和实验记录
- `.athena/logs/agents/`：Layer 1、Layer 2、PREPARE、假设执行和 VALIDATE 原始日志
- `console.log`：完整 headless 控制台输出

原始运行日志包含任务文本、数据路径和模型工具记录，适合团队内部复核；不要把 API key、`.env` 或包含敏感数据的运行目录提交到 PR。

## 10. 结论

本次 SUPPORT2 真实冒烟测试表明：任务完整性 wrapper、评价策略冻结、四维批量假设排序、确定性 Selector、实验执行和预测完整性检查均已在真实数据上工作。`num_leaves=15` 在本轮固定验证切分上优于基线约 `0.0047` AUROC。剩余阻塞点是 VALIDATE 阶段的结构化输出兼容性，应在后续修复或明确记录为已知限制后，再决定是否将该运行标记为完整端到端成功。
