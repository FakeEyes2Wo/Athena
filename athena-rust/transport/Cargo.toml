[package]
name = "transport"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
tokio = { version = "1", features = ["sync", "macros", "rt"] }
tracing = "0.1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
