# Athena Agent Instructions

Before implementing repository changes, read `codex_docs/CURRENT.md`.

- Read only the active plan and supporting documents linked from
  `codex_docs/CURRENT.md`; do not scan all of `codex_docs` for instructions.
- Follow the active plan task by task and update its checkboxes as work is
  completed.
- Preserve unrelated worktree changes and stage only files owned by the
  current task.
- After every acceptance criterion has fresh verification evidence, write the
  completion report, delete the completed implementation plan, and update
  `codex_docs/CURRENT.md` so it no longer points at that plan.
