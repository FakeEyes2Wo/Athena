{
  "name": "athena-ts",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -p packages/athena-core && tsc -p packages/athena-agent && tsc -p packages/athena-research",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
