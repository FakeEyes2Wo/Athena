# Test fixtures

Place small, deterministic datasets and mock tool responses here.

`research_tree_v2.json` is the sole persisted research-graph fixture. Runtime
loading accepts its version-2 shape directly and rejects missing, older, or
unknown schema versions.
